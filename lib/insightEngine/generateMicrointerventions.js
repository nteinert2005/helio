export function generateMicroInterventions(input){
    // const symptoms = input.events.filter(
    //     e => e.type === "symptom" && e.severity && e.severity > 0
    // )

    // return symptoms.map(e => ({
    //     symptom: e.symptom || "unknown", 
    //     severity: e.severity || 0, 
    //     suggestion: "Increase hydration and log symptom again tomorrow."
    // }))

    return "No microinterventions"
}