export function detectAnomalies(input){
    // return input.events
    //     .filter(e => e.type == "weight" && Math.random() < .1)
    //     .map(e => ({
    //         type: "weight_spike",
    //         ts: e.ts,
    //         details: "Possible sudden weight change detected.",
    //         probability: 0.65 
    //     }));

    return 150
}